import alt from "alt-instance";
class PortfolioActions {
	concatPortfolio(){
		return dispatch =>{
			
			
			
			dispatch({data: "Привет"})
		}
	}
}
export default alt.createActions(PortfolioActions)